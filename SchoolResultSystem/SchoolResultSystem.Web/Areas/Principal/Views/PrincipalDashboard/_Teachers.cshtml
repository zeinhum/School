 @model List<SchoolResultSystem.Web.Models.UserModel>;
 <!-- Teachers Card -->
        <div class="card">
            <h2>Teachers Assigned</h2>

            @if (Model!= null && Model.Any())
            {
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Teacher ID</th>
                            <th>Teacher Name</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var teacher in Model)
                        {
                            <tr>
                                <td>@teacher.UserId</td>
                                <td>@teacher.FullName</td>
                                <td>
                                    <button class="btn btn-edit" data-action="changepassword" data-id="@teacher.UserId">
                                        Change Password
                                    </button>
                                    <button class="btn btn-delete" data-action="deactivateteacher" data-id="@teacher.UserId">
                                        Deactivate
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p>No teachers are currently assigned.</p>
            }
        </div>